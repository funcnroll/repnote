import { StatisticsWeeks } from "@/types/StatisticsWeeks";

//  TODO: Implement function to stick to DRY

export function makeData(weeks: StatisticsWeeks, value: string) {
  // if (typeof value === "number")
  //   Object.values(weeks).map((week, index) => {
  //     return {
  //       value: week.reduce((acc, workout) => acc + workout, 0),
  //       index: index + 1,
  //     };
  //   });
  // Object.values(weeks).map((week, index) => {
  //   return {
  //     value: value,
  //   };
  // });
}
